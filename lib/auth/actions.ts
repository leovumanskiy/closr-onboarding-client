'use server'

// Login and logout are now handled by NextAuth.
// Login:  signIn('credentials', { username, password, redirect: false })  in app/login/page.tsx
// Logout: signOut({ callbackUrl: '/login' })  in components/layout/UserMenu.tsx
